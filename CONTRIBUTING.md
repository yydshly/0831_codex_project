# 贡献与维护指南

这个仓库以“来源清楚、结论可验证、索引易维护”为基本原则。

## 新增一个研究条目

1. 在根 README 的研究索引中领取下一个三位编号，例如 `010`。编号一经发布不修改、不复用，也不因展示顺序变化而重排。
2. 将 `research/_template/` 复制为新的研究目录。单仓研究默认命名为 `research/<owner>-<repository>/`；多来源、网页来源或主题研究使用含义明确且稳定的 `kebab-case` slug。
3. 在条目 README 中列出全部上游与来源；单仓目录保留仓库所有者以避免重名。
4. 完成条目 README 中的研究编号、上游地址、基线 commit/tag、许可证、状态与研究问题。
5. 按需增加以下目录，不需要时不要创建空目录：
   - `notes/`：专题笔记；
   - `experiments/`：用于验证结论的最小实验；
   - `patches/`：可复用补丁；
   - `assets/`：自制图、已获授权或符合上游条款的素材。
6. 在根目录 `README.md` 的研究索引中按编号添加一行；如有 Demo，在 Web Demo 索引中使用同一研究编号。

## 研究状态

- `planned`：已收录，尚未开始系统研究；
- `studying`：正在阅读、验证或整理；
- `validated`：核心结论已有代码、测试或可靠资料支持；
- `archived`：暂停维护，仅保留历史记录。

状态描述研究进度，不表示对上游项目质量的评级。

## 使用上游源码

- 默认只提交原创笔记、最小验证代码、补丁和合规素材。
- 本地临时克隆的完整上游源码放在被忽略的 `sources/` 中。
- 不要把其他仓库的 `.git` 目录复制进来。
- 确需保留完整历史时，应先明确选择 Git submodule 或 subtree，并在对应研究条目中说明原因。
- 引用或修改上游代码时，保留原许可证、版权头和来源链接。

## 新增一个 Web Demo

1. 在 `apps/<app-slug>/` 创建自包含项目，并添加该应用自己的 README。
2. 在应用 README 中链接对应研究条目，写明启动、构建和验证方式。
3. 静态构建建议统一使用 `npm run build`，输出到应用内的 `dist/`；其他技术栈需要在 README 中写明等价命令。
4. 为 GitHub Pages 子路径配置资源基地址，避免假设应用部署在域名根目录。
5. 在根目录 `README.md` 的 Web Demo 索引中添加一行。

本仓库只维护 `.github/workflows/pages.yml` 这一条 GitHub Pages 聚合发布流程。新增 Demo 时扩展现有工作流的构建、缓存路径和 artifact 汇总步骤，不要创建另一个 Pages 部署工作流覆盖同一个站点。

## 展示图片与视频

- 每个已发布研究至少准备一张能说明核心结论的封面或页面截图，放在该研究条目的 `assets/` 或 `evidence/` 中。
- 根 README 只保留少量精选展示，并让图片链接到研究报告或在线 Demo；完整展示留在子项目中。所有图片都要提供能说明内容的替代文本。
- 图片优先使用经过压缩的 WebP、JPEG 或 PNG。视频优先使用封面图链接到 GitHub Releases 或外部播放页，避免把大型视频直接写入 Git 历史。
- 所有第三方图片、视频和商标都要在研究条目中记录来源、许可证或合理使用边界；不得提交未获授权的素材。

## 提交前检查

- 根索引和条目状态是否同步；
- 上游地址、研究基线、许可证和日期是否完整；
- 文档中的相对链接是否有效；
- 实验是否有可复现步骤；
- 是否误提交密钥、`.env`、完整上游源码、依赖或构建产物。

建议提交信息使用简洁前缀：`research:`、`demo:`、`docs:` 或 `chore:`。
